# 本机 ChatGPT Helm 连接凭据模板
#
# 复制为 ~/.dsh/.credentials.yaml 后填入真实值（或直接在已有文件中追加两行）。
# 该文件含机密，禁止提交到任何仓库（.gitignore 已排除）；文件权限建议 600。
#
# 获取方式（OpenAI Platform）：
#   CONTROL_PLANE_TUNNEL_ID — 创建 Secure MCP Tunnel 时生成的 tunnel id（tunnel_xxx）
#   CONTROL_PLANE_API_KEY   — Runtime API key，需 Tunnels Read + Tunnels Use 权限（sk-...）
#   详见 docs/connector-creation.md（每台机器独立隧道 + 连接器的完整流程）。

# CONTROL_PLANE_TUNNEL_ID: tunnel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# CONTROL_PLANE_API_KEY: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
